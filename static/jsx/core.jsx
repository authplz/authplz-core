'use strict';

const mountNode = 'root'

$(document).ready(function () {
    console.log("Rendering React components")

    // Render elements
    //ReactDOM.render(<CreateUserForm />, document.getElementById(mountNode));
})

